#parse("SimpleCopyrightFileHeader.h")
#[[#include]]# "${HEADER_FILENAME}"

namespace ${PROJECT_NAME} {

    ${NAME}::${NAME}() = default;
    
    ${NAME}::~${NAME}() = default;

} // namespace ${PROJECT_NAME}