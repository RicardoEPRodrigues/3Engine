Dependencies are ignored to reduce the size of the project.

Two libraries are needed, freeglut and glew.

Create a folder named `dependencies` and place inside folders named as shown:

* freeglut-mingw -> For Windows compilation using mingw
* freeglut-msvc -> For Windows compilation using virtual c++ compiler
* glew-msvc -> For Windows compilation using virtual c++ compiler